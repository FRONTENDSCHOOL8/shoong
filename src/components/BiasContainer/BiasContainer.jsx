import Bias from '../Bias/Bias';

export default function BiasContainer() {
  return (
    <div className="biasContainer bg-primary flex items-center gap-3 w-[360px] h-[100px] px-4 overflow-x-scroll">
      <Bias />
    </div>
  );
}
